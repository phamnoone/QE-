Run file:
sudo chmod +x install.sh
sudo chmod 777 *.gz
sudo chmod 777 qe.tgz
sudo ./install.sh |& tee -a log

REQUIRE: after install complete. close terminal and reopen this.

#take a coffee and waiting. if you richkid can you buy me more cup
Let's enjoy